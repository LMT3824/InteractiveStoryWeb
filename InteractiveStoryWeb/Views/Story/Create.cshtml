@model InteractiveStoryWeb.ViewModels.StoryCreateViewModel

@{
    ViewData["Title"] = "Đăng truyện mới";
}

<style>
    /* Đưa số ký tự vào trong textarea */
    .textarea-wrapper {
        position: relative;
        width: 100%; /* Chiều rộng của textarea */
        margin: 0 auto;
    }

        .textarea-wrapper textarea {
            padding-bottom: 30px; /* Tạo khoảng trống cho số ký tự ở dưới */
        }

        .textarea-wrapper .char-counter {
            position: absolute;
            bottom: 10px;
            right: 10px;
            color: #666;
            font-size: 14px;
            background: rgba(255, 255, 255, 0.8); /* Nền mờ để dễ đọc */
            padding: 2px 5px;
            border-radius: 3px;
        }

    /* Khung ảnh bìa */
    .cover-image-preview {
        width: 210px;
        height: 280px;
        border: 1px solid #e0e0e0;
        border-radius: 5px;
        overflow: hidden;
        margin-top: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
        background-color: #f8f9fa;
    }

        .cover-image-preview img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
</style>


<h2 class="mb-4">@ViewData["Title"]</h2>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        @TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        @TempData["ErrorMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<form asp-action="Create" enctype="multipart/form-data" method="post">
    <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

    <div class="form-group mb-3">
        <label asp-for="Title" class="form-label">Tiêu đề truyện</label>
        <input asp-for="Title" class="form-control" placeholder="Nhập tiêu đề truyện..." />
        <span asp-validation-for="Title" class="text-danger"></span>
    </div>

    <div class="form-group mb-3">
        <label asp-for="Genre" class="form-label">Thể loại</label>
        <select asp-for="Genre" class="form-select" asp-items="@(new SelectList(ViewBag.Genres, "Name", "Name"))">
            <option value="">-- Chọn thể loại --</option>
        </select>
        <span asp-validation-for="Genre" class="text-danger"></span>
    </div>

    <div class="form-group mb-3">
        <label asp-for="Description" class="form-label">Mô tả truyện</label>
        <div class="textarea-wrapper">
            <textarea asp-for="Description" class="form-control" id="DescriptionCreate" rows="5" placeholder="Nhập mô tả truyện..." maxlength="1000"></textarea>
            <span class="char-counter" id="charCounterCreate">0/1000</span>
        </div>
        <span asp-validation-for="Description" class="text-danger"></span>
    </div>

    <div class="form-group mb-3">
        <label asp-for="CoverImage" class="form-label">Ảnh bìa</label>
        <input asp-for="CoverImage" type="file" class="form-control" id="coverImageInput" accept="image/*" />
        <small class="form-text text-muted">Chọn ảnh bìa (định dạng: jpg, png, ...)</small>
        <small class="form-text text-muted">Lưu ý: hãy dùng ảnh có định dạng 210x280 để có được chất lượng tốt nhất.</small>
        <div class="cover-image-preview">
            <img src="/images/ImageNotFound.png" id="coverImagePreview" alt="Ảnh bìa truyện" />
        </div>
    </div>


    <div class="form-check mb-3">
        <input asp-for="AllowCustomization" class="form-check-input" />
        <label asp-for="AllowCustomization" class="form-check-label">Cho phép người đọc tùy chỉnh tên và xưng hô</label>
    </div>

    <button type="submit" class="btn btn-primary">📖 Đăng truyện</button>
    <a asp-action="Index" class="btn btn-secondary ms-2">⬅️ Quay lại</a>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        $(document).ready(function () {
            // Cập nhật số ký tự trong textarea
            function updateCharCounter() {
                var descriptionLength = $('#DescriptionCreate').val().length;
                $('#charCounterCreate').text(descriptionLength + '/1000');
            }

            // Lắng nghe sự kiện input trên textarea
            $('#DescriptionCreate').on('input', function () {
                updateCharCounter();
            });

            // Cập nhật số ký tự ngay khi tải trang
            updateCharCounter();

            // Hiển thị ảnh bìa ngay khi chọn file
            $('#coverImageInput').on('change', function (e) {
                var file = e.target.files[0];
                if (file && file.type.startsWith('image/')) {
                    var reader = new FileReader();
                    reader.onload = function (e) {
                        $('#coverImagePreview').attr('src', e.target.result);
                    };
                    reader.readAsDataURL(file);
                } else {
                    $('#coverImagePreview').attr('src', '/images/ImageNotFound.png');
                }
            });
        });
    </script>
}