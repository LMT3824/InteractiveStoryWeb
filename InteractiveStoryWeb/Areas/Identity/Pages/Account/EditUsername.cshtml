@page
@model EditUsernameModel
@{
    ViewData["Title"] = "Đổi tên người dùng";
}

<link href="https://fonts.googleapis.com/css2?family=STIX+Two+Text:wght@400;700&display=swap" rel="stylesheet">

<style>
    html, body {
        margin: 0;
        padding: 0;
        height: 100vh;
        overflow-x: hidden;
    }

    .background-container {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(to right, #FFE6CE 50%, #FFFBF8 50%);
        z-index: -1;
    }

    .main-content {
        position: relative;
        min-height: 100vh;
        display: flex;
        justify-content: center;
        width: 100%;
        margin-top: 20px;
    }

    .edit-username-container {
        width: 620px;
        height: 480px;
        background-color: #ffffff;
        border: 1px solid #e0e0e0;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

        .edit-username-container h2 {
            font-size: 42px;
            color: #000000;
            margin-bottom: 30px;
            font-family: 'STIX Two Text', serif;
            font-weight: 700;
        }

    .form-group {
        margin-bottom: 20px;
        width: 500px;
    }

        .form-group label {
            display: block;
            color: #000000;
            font-size: 20px;
            margin-bottom: 5px;
        }

        .form-group input {
            width: 100%;
            height: 60px;
            border: 2px solid #672B00;
            border-radius: 5px;
            font-size: 18px;
            padding: 10px;
            box-sizing: border-box;
        }

    .btn-primary {
        width: 180px;
        height: 50px;
        background-color: #5B2A00;
        color: #ffffff;
        border: none;
        border-radius: 5px;
        font-size: 23px;
        cursor: pointer;
        display: block;
        margin: 20px auto;
    }

        .btn-primary:hover {
            background-color: #4a2300;
        }

    .text-danger {
        font-size: 16px;
        color: #ff0000;
        text-align: center;
        margin-top: 10px;
    }
</style>

<div class="background-container"></div>
<div class="main-content">
    <div class="edit-username-container">
        <h2>@ViewData["Title"]</h2>

        <form method="post">
            <!-- New Username -->
            <div class="form-group">
                <label asp-for="Input.NewUsername">Tên người dùng mới</label>
                <input asp-for="Input.NewUsername" class="form-control" autocomplete="username" aria-required="true" placeholder="Tên người dùng mới..." />
                <span asp-validation-for="Input.NewUsername" class="text-danger"></span>
            </div>

            <!-- Password -->
            <div class="form-group">
                <label asp-for="Input.Password">Mật khẩu</label>
                <input asp-for="Input.Password" class="form-control" type="password" autocomplete="current-password" aria-required="true" placeholder="Mật khẩu..." />
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>

            <button type="submit" class="btn btn-primary">ĐỔI TÊN</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}